review of golang servers, http.ListenAndServe, golang servemux, setting headers, Header().Set, Header.Get, getting URL parameters in golang, godoc.org/net/url, URL.path, whole URL request RequestURI, sending a file with http.ServeFile(r, w, "name of file"), the file always come from the folder that's running the programming, the working directory of the program, the web root, YOU HAVE TO RUN YOUR BINARY FROM YOUR ROOT (be in your root and then run your binary), you can use BIN-DATA (on github, search for github golang bin-data) to package all of your files into your binary, reading code to be a better coder, looking at golang source code